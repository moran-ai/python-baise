# 虽然不能修改元组的元素,但是可以给存储元组的变量重新赋值
dimensions = (200,50)
for dimension in dimensions:
    print(dimension)

# 修改元组变量,重新赋值
dimensions = (400,100)
for dimension in dimensions:
    print(dimension)

